class Circle extends Shape {

    /**
     * Creates an instance of Circle.
     * @param {Point} center 
     * @param {Double} radius 
     * @memberof Circle
     */
    constructor(center, radius) {
        this.center = center
        this.radius = radius
    }

    
}
